Build acceptance:
  stage: Build
  before_script:
    - export DOCKER_CONFIG=/tmp/docker-config-$CI_JOB_ID
    - mkdir -p $DOCKER_CONFIG && echo $DOCKER_AUTH_CONFIG > $DOCKER_CONFIG/config.json
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - export GIT_COMMIT_HASH=$(git rev-parse HEAD)
    - export GIT_TAG_NAME=$(git describe --tags)
    - export DOCKER_CACHE_BRANCH_TAG="${CI_COMMIT_REF_SLUG}"
    - export DOCKER_CACHE_LATEST_TAG="review-latest"
    - export JQ_BIN_PATH="./jq"
    - export IMAGE_PREFIX="${CI_REGISTRY_IMAGE}/"
    - export IMAGE_TAG_1="${GIT_TAG_NAME}"
    - export IMAGE_TAG_2="acc-latest"
    - export IMAGE_TAG_3="${CI_COMMIT_REF_SLUG}"
  script:
    - echo -n "${CI_REGISTRY_IMAGE}" > ci_build_image_registry.txt
    - echo -n "${IMAGE_TAG_1}" > ci_build_image_tag.txt
    - echo -n "${IMAGE_TAG_1}" > ci_build_image_version.txt
    - docker version
    - ./scripts/install-jq.sh
    - ./scripts/build/build.sh
  artifacts:
    paths:
      - ci_build_image_registry.txt
      - ci_build_image_tag.txt
      - ci_build_image_version.txt
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "master" && $CI_PROJECT_PATH == "commonground/nlx/nlx"'
  tags:
    - cg
    - shell
