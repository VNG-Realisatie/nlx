Deploy test:
  stage: Deploy
  environment:
    name: test
  before_script:
    - kubectl config use-context delta.k8s.nlx.io
  script:
    - skaffold run --profile test
  only:
    - master
  tags:
    - nlx-privileged
    - shell
