apiVersion: v1
kind: Service
metadata:
  name: {{ include "apps-link-page.fullname" . }}
  labels:
    {{- include "apps-link-page.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "apps-link-page.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
