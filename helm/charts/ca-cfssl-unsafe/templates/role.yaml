{{- if and .Values.rbac.create .Values.config.generateCertificate -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "ca-cfssl-unsafe.fullname" . }}
  labels:
    {{- include "ca-cfssl-unsafe.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - create
      - delete
{{- end -}}
