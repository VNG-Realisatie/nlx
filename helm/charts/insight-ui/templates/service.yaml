apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-ui.fullname" . }}
  labels:
    {{- include "insight-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "insight-ui.selectorLabels" . | nindent 4 }}
