apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "manage-citizens-ui.fullname" . }}
  labels:
    {{- include "manage-citizens-ui.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "manage-citizens-ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "manage-citizens-ui.selectorLabels" . | nindent 8 }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ include "manage-citizens-ui.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ template "manage-citizens-ui.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: BASISREGISTER_FICTIEVE_PERSONEN_API_BASE_URL
              value: {{ .Values.basisregisterFictievePersonenApiBaseUrl }}
            - name: ORGANIZATION_NAME
              value: {{ .Values.organizationName }}
            - name: ORGANIZATION_LOGO
              value: {{ .Values.organizationLogo }}
          ports:
            - name: http
              containerPort: 3012
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
