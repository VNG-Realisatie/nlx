apiVersion: v1
kind: Service
metadata:
  name: {{ include "manage-citizens-ui.fullname" . }}
  labels:
    {{- include "manage-citizens-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manage-citizens-ui.selectorLabels" . | nindent 4 }}
