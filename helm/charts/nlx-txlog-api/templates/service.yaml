apiVersion: v1
kind: Service
metadata:
  name: {{ include "txlog-api.fullname" . }}
  labels:
    {{- include "txlog-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "txlog-api.selectorLabels" . | nindent 4 }}
