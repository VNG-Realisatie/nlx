apiVersion: v1
kind: Service
metadata:
  name: {{ include "parkeerrechten-api.fullname" . }}
  labels:
    {{- include "parkeerrechten-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parkeerrechten-api.selectorLabels" . | nindent 4 }}
