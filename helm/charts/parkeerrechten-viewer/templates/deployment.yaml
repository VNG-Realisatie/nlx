apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "parkeerrechten-viewer.fullname" . }}
  labels:
    {{- include "parkeerrechten-viewer.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "parkeerrechten-viewer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "parkeerrechten-viewer.selectorLabels" . | nindent 8 }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ include "parkeerrechten-viewer.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ template "parkeerrechten-viewer.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: PARKEERRECHTEN_API_BASE_URL
              value: {{ .Values.parkeerrechtenApiBaseUrl }}
            - name: ORGANIZATION_NAME
              value: {{ .Values.organizationName }}
            - name: ORGANIZATION_LOGO
              value: {{ .Values.organizationLogo }}
            - name: ORGANIZATION_COLOR_PRIMARY
              value: {{ .Values.organizationColor }}
          ports:
            - name: http
              containerPort: 3010
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
