apiVersion: v1
kind: Service
metadata:
  name: {{ include "parkeerrechten-viewer.fullname" . }}
  labels:
    {{- include "parkeerrechten-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parkeerrechten-viewer.selectorLabels" . | nindent 4 }}
