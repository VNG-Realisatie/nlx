apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "brp.fullname" . }}-nlxctl
  labels:
    {{- include "brp.labels" . | nindent 4 }}
data:
  root.pem: {{ (required "Root certificate PEM is required" .Values.global.tls.rootCertificatePEM) | quote }}
