apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-websockets-proxy.fullname" . }}
  labels:
    {{- include "nginx-websockets-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-websockets-proxy.selectorLabels" . | nindent 4 }}
