apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rdw.fullname" . }}-postgresql-init-script
  labels:
    {{- include "rdw.labels" . | nindent 4 }}
data:
  create-management-database.sql: 'CREATE DATABASE nlx_management;'
