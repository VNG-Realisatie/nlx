apiVersion: cert-manager.io/v1beta1
kind: Issuer
metadata:
  name: {{ include "saas-org-x.fullname" . }}
  labels:
    {{- include "saas-org-x.labels" . | nindent 4 }}
spec:
  ca:
    secretName: {{ include "saas-org-x.fullname" . }}-ca-issuer
